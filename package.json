{
  "name": "blog-docker",
  "version": "1.0.0",
  "description": "docker container for wordpress",
  "main": "index.js",
  "scripts": {
    "wp:up": "docker-compose up",
    "wp:down": "docker-compose down --volumes",
    "docker:check": "systemctl status docker",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
